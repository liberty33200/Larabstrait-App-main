version: '3.8'

services:
  larabstrait:
    build: .
    container_name: larabstrait-app
    restart: unless-stopped
    ports:
      - "3050:3000"
    environment:
      - NODE_ENV=production
      # On fait le pont pour Microsoft
      - MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID}
      - MICROSOFT_TENANT_ID=${MICROSOFT_TENANT_ID}
      - MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET}
      - EMAIL_STUDIO=${EMAIL_STUDIO}
      # On fait le pont pour l'URL de l'app et les notifications
      - REDIRECT_URI=${REDIRECT_URI}
      - REDIRECT_URL=${REDIRECT_URL}
      - APP_URL=${APP_URL}
      - DATAVERSE_URL=${DATAVERSE_URL}
      - SESSION_SECRET=${SESSION_SECRET}
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}
      - ABBY_API_KEY=${ABBY_API_KEY}
      - abby_api_key=${abby_api_key}
    volumes:
      # On lie le dossier interne /app/data au dossier visible du NAS
      - /volume1/docker/larabstrait-app/data:/app/data